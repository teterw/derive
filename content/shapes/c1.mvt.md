# c1.mvt

Question shapes seen in real papers and textbook exercises, and which
generator covers each. See `docs/CONTENT-PIPELINE.md` section 2.

## c1.rolle
- [x] A parabola through two points at the same height -> `c1.rolle`
- [x] The same, lifted off the axis -> `c1.rolle`
- [x] The height of the flat point rather than its position -> `c1.rolle`
- [x] A wider interval, where counting on will not find it -> `c1.rolle`
- [ ] A cubic with two flat points, only one inside the interval
- [ ] A function that fails a hypothesis, so the theorem says nothing
- [ ] Showing an equation has exactly one root
- [ ] Rolle applied to `f'` to say something about `f''`

## c1.mean-value
- [x] A quadratic, where the point is the midpoint -> `c1.mean-value`
- [x] A quadratic with a linear term as well -> `c1.mean-value`
- [x] A cubic, where the point is irrational -> `c1.mean-value`
- [x] A journey, where the average speed is achieved -> `c1.mean-value`
- [ ] A function with a square root in it
- [ ] An interval where the theorem does not apply, and why
- [ ] Two points on a curve with a parallel tangent found graphically
- [ ] Using the theorem to prove an inequality

## c1.mvt-bound
- [x] The largest a function can reach from a known start -> `c1.mvt-bound`
- [x] The smallest it can reach -> `c1.mvt-bound`
- [x] The largest possible change, with no starting value -> `c1.mvt-bound`
- [x] A wider interval, where the bound grows with it -> `c1.mvt-bound`
- [ ] A bound on `f'` that varies across the interval
- [ ] Showing two functions with the same derivative differ by a constant
- [ ] Bounding an error in a linear approximation
- [ ] A bound that rules a claimed value out
