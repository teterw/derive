# trig.ratios

Question shapes seen in real papers and textbook exercises, and which
generator covers each. See `docs/CONTENT-PIPELINE.md` section 2.

## trig.definition
- [x] All three sides given, ratio at either acute angle -> `trig.definition`
- [x] Larger Pythagorean triples -> `trig.definition`
- [x] One side withheld, so Pythagoras comes first -> `trig.definition`
- [x] Withheld side at the harder triples -> `trig.definition`
- [ ] Given one ratio, find another for the same angle
- [ ] A triangle whose sides are not a whole-number triple
- [ ] Two angles of the same triangle compared

## trig.special
- [x] A single value at 30, 45 or 60 -> `trig.special`
- [x] Including 0 and 90, where one ratio has no value -> `trig.special`
- [x] A sum of two values -> `trig.special`
- [x] A product of two values, with a coefficient -> `trig.special`
- [ ] A quotient of two values
- [ ] Verifying an identity such as sine squared plus cosine squared
- [ ] Working backwards: which angle has this value

## trig.solve
- [x] Hypotenuse and a special angle, find a leg -> `trig.solve`
- [x] Either leg from the hypotenuse -> `trig.solve`
- [x] Adjacent and an angle, find the opposite -> `trig.solve`
- [x] Angle of elevation with an eye height -> `trig.solve`
- [ ] Angle of depression, looking down
- [ ] Two triangles sharing a side
- [ ] Finding the angle rather than the side
- [ ] A bearing or a slope expressed as a ratio
